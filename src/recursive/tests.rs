#[test]
#[ignore]
fn smoke_test() {
    // let parser = RecursiveParser::new(|r| {
    //     '['.then(r)
    //         .then(']')
    //         .map(|((_, depth), _)| depth + 1)
    //         .or('-'.map(|_| 0))
    // });

    // let parsed = parser.parse_all("[[[-]]]").unwrap();
    // assert_eq!(parsed, 3);
    todo!()
}
