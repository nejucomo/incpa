[package]
name = "incpa"
version = "0.0.1"
edition = "2021"

[features]
tokio = ["dep:tokio"]

[dependencies]
derive-new = "0.7.0"
either = "1.13.0"
tokio = { version = "1.42.0", optional = true, default-features = false, features = ["io-util"] }
