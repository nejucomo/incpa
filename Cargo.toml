[package]
name = "incpa"
version = "0.1.0"
edition = "2021"

[features]
tokio = ["dep:tokio"]

[dependencies]
derive-new = "0.7.0"
tokio = { version = "1.42.0", optional = true, default-features = false, features = ["io-util"] }
